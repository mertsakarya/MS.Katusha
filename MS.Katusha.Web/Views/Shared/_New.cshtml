@using MS.Katusha.Enumerations
@{ var profiles = ViewBag.KatushaNewProfiles as IEnumerable<ProfileModel>; }
@if (profiles != null) {
    <h3>New Users</h3>
    <div id="newgallery" data-toggle="modal-gallery" data-target="#modal-gallery">
        @foreach (var profile in profiles) {
            @Html.DisplayProfilePhoto(profile, PhotoType.Icon, "newgallery", true)
        }
    </div>
    <a class="btn btn-warning cancel mixpanel" mp-event="Click New Profiles" mp-data="{guid:profile.guid, name:profile.name}" href='@Url.Action("New", "Profiles")' title="All users registered recently">
        All new users...
    </a>
}
